#include<stdio.h>
int main()
{
    int number;
    printf("Enter any decimel number:");
    scanf("%d",&number);
    printf("hexa_decimal:%x",number);//use %x foramte spacifier for conversion to decimel to hexa decimel.
    return 0;
}