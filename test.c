#include<stdio.h>
int main()
{
    int a;
    float i;
    double m;
    char a1;
    printf("size of int=%d bytes\n",sizeof(a));
    printf("size of float=%d bytes\n",sizeof(i));//hare i use %d in float value because i want intiger number 
    printf("size of double=%d bytes\n",sizeof(m));
    printf("size of char=%d byte\n",sizeof(a1));

    return 0;
    
    
    
}